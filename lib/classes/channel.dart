import 'package:flutter/material.dart';

class ChannelType {
  String? name;
  num? id;
  String? tagline;
  String? color;
  String? image;

  ChannelType({this.name, this.id, this.tagline, this.color, this.image});
}